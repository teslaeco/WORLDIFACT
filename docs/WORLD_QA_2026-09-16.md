# WORLDIFACT five-world QA — 2026-09-16

Status: **PARTIAL PASS / UX FIX PREPARED / PRODUCTION DEPLOYMENT PENDING OWNER APPROVAL**.

This audit separates HTTP/source evidence from owner-device evidence. No new paid GPT-6 Astra or Oracle generation is authorized by this work; the approved 4/4 LIVE pilot remains exhausted.

| World | Route | Current evidence | Status |
| --- | --- | --- | --- |
| Chess Cube 512 AI | `/chess` | Central portal route exists and intentionally hands off to the public Chess Cube site. Owner reported the other world pages working during Android QA; a dedicated final screenshot is still useful. | PARTIAL PASS |
| Terra — Fix ISS | `/iss` | Copied first-party ISS application is assembled into the release and checked by the release manifest. Owner reported the other world pages working during Android QA. | PARTIAL PASS |
| 8 Planets in 8 Days | `/planets` | Owner Android screenshot at 13:15 showed the local no-login mini test rendering correctly. Owner requested the main expedition view to be restored and the mini test moved to a secondary tab. This branch implements that UX. | MINI TEST PASS / NEW UX PENDING DEPLOY |
| Enchanted AI Shop | `/shop` | Public shop/reference route remains connected. Owner reported the other world pages working during Android QA. Manufacturing claims remain validation-required. | PARTIAL PASS |
| AI Game Lab | `/lab` | Native WebGL scene loads on owner Android; portal navigation hotfix was deployed. No-cost DEMO is available while LIVE quota is exhausted. This branch adds a visible World #5 Forge Studio reference tab and three no-cost example presets. | PARTIAL PASS / NEW TAB PENDING DEPLOY |

## Automated release evidence

The production release checker already requests every primary portal route over HTTPS, verifies the SPA bytes, checks the copied Chess/ISS/Terra application manifest and files, validates `/api/health`, runs a DEMO blueprint request, and confirms cross-origin rejection. Production run `35088686381` completed successfully for commit `4a83543fba174e9a66eb53356798f99fa448b86c`.

This is HTTP/build evidence, not a replacement for physical Android WebGL QA.

## Prepared 8 Planets UX

The primary `/planets` view is now the WORLDIFACT expedition screen with all eight stages and per-planet mission/hazard detail. The existing simple Move/Jump interaction is retained as a **Mini test** tab. The older external FORGE World Builder remains a separate reference link because its cross-site iframe was visibly unreliable on Android.

Truth boundary: the expedition screen is a WORLDIFACT DEMO view and the full eight-level platforming campaign remains **PLANNED**.

## AI Game Lab World #5 reference

The native P0 workbench remains the first tab. A visible external tab links to the public Forge Studio reference from project instructions, with the ForgeMCP source available beside it. The public reference is not presented as the native Astra P0 implementation.

## No-cost generated examples

Three local examples are wired into Game Lab and into automated tests:

1. `Forest rover workshop` → valley scene, blue rover, village/forest cues.
2. `Lunar repair base` → lunar scene.
3. `Neptune maker town` → ocean scene.

Each example uses `demoBlueprint` + `localSceneResult`, therefore returns `DEMO / MOCK`, `model=null`, and a MAKE plan forced to `validation-required`. These examples make **zero OpenAI/Oracle calls**.

## Re-test after deployment

- `/planets`: default expedition view appears; Mini test tab opens and Move/Jump works; external FORGE link opens separately.
- `/lab`: native P0 scene appears; each no-cost example changes the scene; World #5 Forge Studio tab opens the public reference; LIVE Astra remains disabled unless a new launch allowance is explicitly approved.
- Re-open `/chess`, `/iss`, `/shop` and `/terra` once more in the same clean Android session and record any visual failure instead of inferring browser PASS from HTTP tests.
