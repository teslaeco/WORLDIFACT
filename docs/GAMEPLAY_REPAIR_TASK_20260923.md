# Queen, meadow and excavation repair — executable task and evidence

## Owner request and limits

Implement the screenshot-driven repair in `teslaeco/WORLDIFACT`, create a PR and merge only after green CI. Preserve the exact current Queen and original fan. The owner also requested a backhoe-loader, a desert patch with actual digging, and their Astra rim. No paid generation, private-archive bypass, payment, order or new contest submission is authorized by this repair.

Baseline: main `76c8f8344cd33e1b0bdc5de32863d2ad26a723c0` (PR #81). The owner's screenshots are negative visual acceptance evidence: the prior synthetic tests did not establish satisfactory original-model animation or appearance.

## Execution task

1. Remove the fixed walking crouch; separate the leg bindings, keep original shoes attached to their own ankles, and synchronize stance/swing with actual travel. Preserve original geometry, indices, UVs and textures. Add pre-jump crouch, strong knee-to-chest tuck during a single double-jump flip, extension and landing absorption.
2. Restore the original fan surface, ribs and handle as one hand-attached object. Remove the added rotor housings/impellers that damaged its silhouette. Recognize unnamed primitives under named source groups. Keep the hand lowered on foot and raised for flight.
3. Replace sparse brown blades with dense green multi-blade clumps and procedural ground cover. Bound mobile geometry; keep river, portals, parked rover and desert clear. Preserve camera, existing five routes, smaller original logos and readable petition CTA.
4. Create a reachable 25 m x 25 m GAME desert at X 15–40, Z 14–39. Remove the underlying flat meadow plane in this patch. Update both mesh vertices and movement height sampling when excavating.
5. Add optional front-loader and rear articulated-backhoe tools to the existing rover. Lowering a bucket must contact soil before collecting it. Transfer a bounded, conserved volume into a visible 650 L bucket; dumping must deposit that material into a new mound. Keep payload through tool/camera/avatar changes, prohibit switching/removing loaded tools, and stop digging on focus loss, reset or exit.
6. Use only the actual owner-approved Astra rim, never the Meshy screenshot or a look-alike. If source bytes cannot be found, expose an honest local original-GLB mount and leave exact automatic installation BLOCKED. Keep original wheel pivots and hollow tyres so they do not cover the imported rim openings.
7. Add regressions for real geometry, contact, volume, poses, source preservation and inputs. Run repository CI and Worker packaging, review exact head/base, then merge with a pinned head SHA. Record actual deployment separately from build success.

## Implemented GAME behavior

- Analytic two-link feet, dynamic stride-dependent pelvis height and slightly separated stance; named whole-foot binding rather than splitting shoes at arbitrary vertex planes. Tuck affects hips/knees/ankles, not only the whole-body flip pivot. Native/fallback paths also receive jump poses.
- Source fan meshes and colors retained; no generated mechanical decoration is attached. Source Queen job remains `99397623-e45c-48dc-95ec-6f84446a54d5`.
- 16,000 mobile or 28,000 desktop five-blade clumps in one instanced draw. Mobile grass budget is 240,000 triangles, not an FPS promise. Green locally generated texture covers the far field. No new network textures or licenses.
- Editable 51 x 51 sand height field with 0.5 m cells, -1.35 m bedrock and bounded deposition. Vertex-volume transfers are conserved; ray intersections and gameplay sampling use the same triangles. Digging is actual local GAME deformation, not particle-only feedback and not engineering soil mechanics.
- Front bucket plus a two-link rear boom, hydraulic visuals, raised carry state, tilt/dump state and visible collected sand. Speed is reduced with attachments and driving into large wheel-height differences is blocked. This is not a complete rigid-body suspension/soil simulator.
- Terrain, active tool and bucket load survive avatar recreation within the same world. Leaving/unmounting the world or starting a structurally different scene resets this local session. No cross-device persistence is claimed.
- Original rim import is local-only, embedded GLB up to 30 MB, static geometry and 200,000 rendered triangles per wheel. Uniform scaling and axis alignment preserve source geometry/materials. Four wheel instances rotate with actual travel; stock solid discs are hidden, tyres remain hollow.

## Verification and release gate

Local TypeScript passes. Lint: 0 errors, 17 existing warnings. Focused new/changed control, rig, terrain and rim tests pass. The frontend production bundle builds locally. The aggregate local test attempt cannot complete the existing Chromium fixture; the second run excluding that fixture passes 433 tests but the existing ISS fixture cannot load its omitted vendor assets before the network-dependent preparation step. These are NOT a full local verify pass. CI must run the unmodified full suite, ISS preparation, HTTP smoke, pinned foundation assembly and Worker dry-run before merge.

The PR records the exact reviewed commit, final counts, green workflows, merge and deployment result. Temporary source-transport workflows/files are removed before review. No deployment or visual success is inferred from test fixtures.

## Outstanding acceptance — do not hide

**NOT VISUALLY VERIFIED:** original authenticated Queen mesh fit, fan grip, motion appearance, new scenery and physical Android frame time. Existing browser/authentication blocks were respected; no alternate browser, CDP or CI world renderer was used. Runtime anatomical binding is still approximate GAME rigging, not artist-reviewed motion capture.

**BLOCKED — exact Astra rim:** repository and available project/library searches recovered the comparison article/screenshots, not the original 3D rim bytes or a verified saved-job reference. The original was not automatically mounted. An imported owner file is labeled owner-provided; its claimed generator is not independently verified. Obtain the exact original GLB or authorized job reference before claiming that this vehicle uses the requested Astra model.
